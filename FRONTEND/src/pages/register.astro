---

import Layout from "../layouts/Acceso.astro"

---

<Layout title="Registro | Sportify">
    <div class="flex flex-col justify-between items-center w-auto h-auto my-auto py-8 px-16 bg-gray-100 dark:bg-stone-950 border rounded-lg border-gray-300 dark:border-black">
        <header class="my-6">
            <h1 class="font-semibold text-4xl">Registro</h1>
        </header>
        <form class="flex flex-col gap-4 my-4">
            <div class="min-w-72">
                <label for="nombre" class="block text-sm text-gray-800 dark:text-gray-200">Nombre:</label>
                <input type="text" id="nombre" required placeholder="Nombre" class="w-full px-4 py-2 bg-white dark:bg-gray-800 border rounded-lg border-gray-400 dark:border-gray-600 focus:border-green-600 focus:ring-green-600 focus:outline-none focus:ring focus:ring-opacity-40" />
            </div>

            <div class="min-w-72">
                <label for="email" class="block text-sm text-gray-800 dark:text-gray-200">Correo electrónico:</label>
                <input type="email" id="email" required placeholder="Correo electrónico" class="w-full px-4 py-2 bg-white dark:bg-gray-800 border rounded-lg border-gray-400 dark:border-gray-600 focus:border-green-600 focus:ring-green-600 focus:outline-none focus:ring focus:ring-opacity-40" />
            </div>

            <div class="min-w-72">
                <label for="password" class="block text-sm text-gray-800 dark:text-gray-200">Contraseña:</label>
                <input type="password" id="password" required placeholder="Contraseña" class="w-full px-4 py-2 bg-white dark:bg-gray-800 border rounded-lg border-gray-400 dark:border-gray-600 focus:border-green-600 focus:ring-green-600 focus:outline-none focus:ring focus:ring-opacity-40" />
            </div>

            <button class="w-full px-6 py-2 font-medium text-white transition-all duration-300 transform bg-green-600 rounded-lg hover:scale-105">
                Registrarse
            </button>
        </form>
        <aside class="flex flex-col items-center gap-1 mt-2">
            <p class="text-green-600 text-xs font-medium">
                ¿Ya tienes una cuenta?
            </p>
            <a href="/login" class="text-black dark:text-gray-200 text-xs font-medium hover:underline">
                Iniciar Sesión
            </a>
        </aside>
    </div>
</Layout>

<script>
    export default {
        data() {
            return {
                nombre: '',
                email: '',
                password: ''
            };
        },
        methods: {
            submitForm() {
                // Aquí puedes enviar los datos del formulario al servidor o realizar otras acciones según tus necesidades
                console.log('Datos enviados:', this.nombre, this.email, this.password);
            }
        }
    };
</script>