---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/sportify-favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="text-black dark:text-white">
        <div class="bg-white dark:bg-gradient-to-b from-black to-stone-800 w-screen h-screen absolute overflow-hidden -z-20">
            <img src="/voleibol-piso-deportista.png" alt="Imagen deportista" class="absolute opacity-60 -z-10 h-[163%]">
        </div>
        <main class="flex flex-col justify-evenly items-center w-screen h-screen">
            <div class="flex justify-center mt-8">
                <h1 class="font-bold text-5xl">Sportify</h1>®
            </div>
            <slot />
        </main>
	</body>
</html>